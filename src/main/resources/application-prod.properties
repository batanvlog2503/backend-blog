# C?u hình Database Railway (S? d?ng bi?n môi tr??ng)
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}?useSSL=true&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Cài ??t Hibernate cho Prod
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# T?i ?u connection pool cho production
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10